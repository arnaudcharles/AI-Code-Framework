# /examples - Runnable usage examples

Use only the public API. Do not call private functions. Keep examples realistic and free of real secrets. Test representative examples as smoke or acceptance tests.
